apiVersion: v1
kind: Service
metadata:
  name: prooflink-api
  namespace: prooflink
  labels:
    app.kubernetes.io/name: prooflink-api
    app.kubernetes.io/part-of: prooflink
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prooflink-api
  ports:
    - port: 80
      targetPort: 3001
      protocol: TCP
